version: '3.5'
services:
  frontend:
    image: jwilder/nginx-proxy:alpine
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
    networks:
    - frontend

networks:
  frontend:
    name: ${FRONTEND_NETWORK}
